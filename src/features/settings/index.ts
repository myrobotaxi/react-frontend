/**
 * Settings feature — public API.
 * Only export what app/ pages and other features need.
 */

// Types
export type { UserSettings, NotificationPreferences } from './types';

// Components will be added as they are built:
// export { SettingsScreen } from './components/SettingsScreen';

// Server actions will be added as they are built:
// export { getSettings } from './api/get-settings';
// export { updateSettings } from './api/update-settings';
