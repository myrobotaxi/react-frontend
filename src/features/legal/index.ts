/**
 * Legal surfaces — public documents the product is obliged to publish.
 *
 * Only the screen is exported. The sections it is assembled from are an
 * implementation detail of one page and have no other call site.
 */
export { PrivacyPolicyScreen } from './components/PrivacyPolicyScreen';
