/**
 * Landing — the public face of the apex domain while the web app is retired.
 *
 * One screen, no state, no data. It exists as a feature module rather than a
 * component so `app/page.tsx` stays a thin orchestrator.
 */
export { ComingSoonScreen } from './components/ComingSoonScreen';
