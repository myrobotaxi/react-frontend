/**
 * Sign-up page — account creation screen.
 * Renders the SignUpForm feature component.
 */
export default function SignUpPage() {
  return (
    <main className="min-h-screen bg-bg-primary flex items-center justify-center px-6">
      <p className="text-text-muted text-sm">Sign Up — coming soon</p>
    </main>
  );
}
