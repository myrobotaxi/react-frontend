/**
 * Sign-in page — minimal dark auth screen.
 * Renders the SignInForm feature component.
 */
export default function SignInPage() {
  return (
    <main className="min-h-screen bg-bg-primary flex items-center justify-center px-6">
      <p className="text-text-muted text-sm">Sign In — coming soon</p>
    </main>
  );
}
