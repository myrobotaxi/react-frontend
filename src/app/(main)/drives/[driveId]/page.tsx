/**
 * Drive summary page — detailed view of a single drive.
 * Fetches drive by ID and passes to DriveSummaryScreen.
 */
export default function DriveSummaryPage() {
  return (
    <main className="min-h-screen bg-bg-primary px-6 pt-16">
      <p className="text-text-muted text-sm">Drive Summary — coming soon</p>
    </main>
  );
}
