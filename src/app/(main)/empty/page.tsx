/**
 * Empty state page — shown when no vehicles are linked.
 * Premium onboarding screen with CTA buttons.
 */
export default function HomeEmptyPage() {
  return (
    <main className="min-h-screen bg-bg-primary flex items-center justify-center px-6">
      <p className="text-text-muted text-sm">Empty State — coming soon</p>
    </main>
  );
}
